---
// import '../styles/global.css'; // Descomenta si tienes CSS global
import { isCartOpen } from '../store/cartStore';

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="es" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="HomeMart - Tecnología, Hogar y Más" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title}</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800&display=swap" rel="stylesheet">
  </head>
  
  <body class="bg-gray-100 font-sans text-slate-800 antialiased flex flex-col min-h-screen">
    
    <header class="sticky top-0 z-50 bg-blue-800 text-white shadow-md">
      <div class="bg-blue-900 text-xs py-1 px-4 text-center text-blue-200">
        Envíos a todo el país 📦 | Compra 100% Segura 🔒
      </div>

      <nav class="container mx-auto px-4 py-3 flex justify-between items-center gap-4">
        <a href="/" class="text-2xl font-black tracking-tighter flex items-center gap-2 italic">
          <span class="bg-white text-blue-800 px-2 rounded-sm not-italic">H</span> HOMEMART
        </a>

        <div class="hidden md:flex flex-1 max-w-xl mx-4 relative">
          <input 
            type="text" 
            placeholder="Buscar productos, marcas y más..." 
            class="w-full pl-4 pr-10 py-2 rounded-sm text-slate-900 focus:outline-none focus:ring-2 focus:ring-yellow-400"
          />
          <button class="absolute right-0 top-0 h-full px-3 bg-yellow-500 hover:bg-yellow-400 text-blue-900 font-bold rounded-r-sm transition">
            🔍
          </button>
        </div>

        <div class="flex items-center gap-6 text-sm font-medium">
          <a href="#" class="hidden sm:block hover:text-yellow-400 transition">Mis Pedidos</a>
          
          <button id="cart-btn" class="flex items-center gap-2 hover:text-yellow-400 transition relative">
            <div class="relative">
              <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="8" cy="21" r="1"/><circle cx="19" cy="21" r="1"/><path d="M2.05 2.05h2l2.66 12.42a2 2 0 0 0 2 1.58h9.78a2 2 0 0 0 1.95-1.57l1.65-7.43H5.12"/></svg>
              </div>
            <span class="hidden sm:inline">Carrito</span>
          </button>
        </div>
      </nav>
      
      <div class="bg-blue-700 text-sm py-2 px-4 overflow-x-auto">
        <div class="container mx-auto flex gap-6 text-blue-100 whitespace-nowrap">
          <a href="#" class="hover:text-white font-bold">Ofertas del Día</a>
          <a href="#" class="hover:text-white">Electrónica</a>
          <a href="#" class="hover:text-white">Hogar</a>
          <a href="#" class="hover:text-white">Muebles</a>
          <a href="#" class="hover:text-white">Deportes</a>
        </div>
      </div>
    </header>

    <main class="flex-grow">
      <slot />
    </main>

    <footer class="bg-slate-900 text-slate-400 py-10 mt-12 text-sm">
      <div class="container mx-auto px-4 grid grid-cols-1 md:grid-cols-4 gap-8">
        <div>
          <h4 class="text-white font-bold mb-4 uppercase">Acerca de HomeMart</h4>
          <p>Tu tienda departamental de confianza. Encuentra todo lo que necesitas para tu hogar y oficina en un solo lugar.</p>
        </div>
        <div>
          <h4 class="text-white font-bold mb-4 uppercase">Ayuda</h4>
          <ul class="space-y-2">
            <li><a href="#" class="hover:text-white">Rastrear Pedido</a></li>
            <li><a href="#" class="hover:text-white">Cambios y Devoluciones</a></li>
          </ul>
        </div>
        <div>
          <h4 class="text-white font-bold mb-4 uppercase">Legal</h4>
          <ul class="space-y-2">
            <li><a href="#" class="hover:text-white">Términos y Condiciones</a></li>
            <li><a href="#" class="hover:text-white">Política de Privacidad</a></li>
          </ul>
        </div>
        <div>
          <h4 class="text-white font-bold mb-4 uppercase">Métodos de Pago</h4>
          <div class="flex gap-2">
            <div class="w-10 h-6 bg-white rounded"></div>
            <div class="w-10 h-6 bg-white rounded"></div>
            <div class="w-10 h-6 bg-white rounded"></div>
          </div>
        </div>
      </div>
      <div class="text-center mt-10 pt-4 border-t border-slate-800">
        &copy; 2025 HomeMart S.A. Todos los derechos reservados.
      </div>
    </footer>

    <script>
      // Script simple para abrir el carrito al hacer click en el icono del header
      import { isCartOpen } from '../store/cartStore';
      const btn = document.getElementById('cart-btn');
      if(btn) btn.addEventListener('click', () => isCartOpen.set(true));
    </script>
    
    <style>
      body { font-family: 'Inter', sans-serif; }
    </style>
  </body>
</html>

