---
import Layout from '../layouts/Layout.astro';

// Datos simulados para mantener el código limpio
const products = [
  {
    title: "Cactus Espina",
    price: "$15.99",
    image: "/cactus1.jpg",
    desc: "Perfecto para interiores luminosos."
  },
  {
    title: "Cactus Bola",
    price: "$12.99",
    image: "/cactus2.jpg",
    desc: "Compacto y fácil de cuidar."
  },
  {
    title: "Cactus Gigante",
    price: "$25.99",
    image: "/cactus3.jpg",
    desc: "Una pieza central impresionante."
  },
];
---

<Layout title="CactusWorld - Naturaleza en tu hogar">
  <section id="home" class="relative min-h-[85vh] flex items-center justify-center overflow-hidden">
    <div class="absolute inset-0 z-0">
      <img 
        src="/hero-cactus.jpg" 
        alt="Fondo Cactus" 
        class="w-full h-full object-cover"
      />
      <div class="absolute inset-0 bg-gradient-to-t from-stone-900/90 via-stone-900/50 to-transparent"></div>
    </div>

    <div class="relative z-10 text-center px-4 max-w-4xl mx-auto mt-20">
      <span class="text-emerald-400 font-semibold tracking-wider uppercase text-sm mb-4 block">Nueva Colección 2024</span>
      <h1 class="text-5xl md:text-7xl font-bold text-white mb-6 leading-tight">
        Lleva la esencia del <br/> <span class="text-emerald-500">desierto</span> a tu hogar.
      </h1>
      <p class="text-xl md:text-2xl text-stone-200 mb-10 max-w-2xl mx-auto font-light">
        Cactus premium seleccionados a mano. Resistentes, hermosos y cultivados de manera sostenible.
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a href="#products" class="bg-emerald-600 text-white py-4 px-8 rounded-full font-semibold hover:bg-emerald-700 transition duration-300 shadow-lg hover:shadow-emerald-500/30">
          Ver Catálogo
        </a>
        <a href="#about" class="bg-white/10 backdrop-blur-sm text-white border border-white/30 py-4 px-8 rounded-full font-semibold hover:bg-white/20 transition duration-300">
          Conócenos
        </a>
      </div>
    </div>
  </section>

  <section class="py-20 bg-stone-50">
    <div class="container mx-auto px-6 grid grid-cols-1 md:grid-cols-3 gap-12 text-center">
      <div class="space-y-4">
        <div class="w-16 h-16 bg-emerald-100 rounded-full flex items-center justify-center mx-auto text-emerald-600 text-2xl">🌿</div>
        <h3 class="text-xl font-bold text-stone-800">100% Orgánicos</h3>
        <p class="text-stone-600">Cultivados sin pesticidas agresivos, respetando el ciclo natural.</p>
      </div>
      <div class="space-y-4">
        <div class="w-16 h-16 bg-emerald-100 rounded-full flex items-center justify-center mx-auto text-emerald-600 text-2xl">🚚</div>
        <h3 class="text-xl font-bold text-stone-800">Envío Seguro</h3>
        <p class="text-stone-600">Empaque especial para asegurar que tu cactus llegue perfecto.</p>
      </div>
      <div class="space-y-4">
        <div class="w-16 h-16 bg-emerald-100 rounded-full flex items-center justify-center mx-auto text-emerald-600 text-2xl">💧</div>
        <h3 class="text-xl font-bold text-stone-800">Fácil Cuidado</h3>
        <p class="text-stone-600">Incluimos guía de cuidados para que tu planta prospere.</p>
      </div>
    </div>
  </section>

  <section id="products" class="container mx-auto px-6 py-24">
    <div class="text-center mb-16">
      <h2 class="text-4xl font-bold text-stone-900 mb-4">Nuestros Favoritos</h2>
      <div class="w-24 h-1 bg-emerald-500 mx-auto rounded"></div>
    </div>
    
    <div class="grid grid-cols-1 md:grid-cols-3 gap-10">
      {products.map((product) => (
        <div class="group bg-white rounded-2xl shadow-sm hover:shadow-xl transition duration-500 overflow-hidden border border-stone-100">
          <div class="relative overflow-hidden h-64">
            <img 
              src={product.image} 
              alt={product.title} 
              class="w-full h-full object-cover transform group-hover:scale-110 transition duration-700" 
            />
            <div class="absolute top-4 right-4 bg-white/90 backdrop-blur px-3 py-1 rounded-full text-sm font-bold text-stone-800">
              {product.price}
            </div>
          </div>
          <div class="p-8">
            <h3 class="text-2xl font-bold text-stone-800 mb-2">{product.title}</h3>
            <p class="text-stone-500 mb-6">{product.desc}</p>
            <a href="#" class="block w-full py-3 border-2 border-emerald-600 text-emerald-700 font-semibold rounded-lg hover:bg-emerald-600 hover:text-white transition duration-300 text-center">
              Añadir al Carrito
            </a>
          </div>
        </div>
      ))}
    </div>
  </section>

  <section id="about" class="bg-emerald-900 py-24 text-white overflow-hidden">
    <div class="container mx-auto px-6">
      <div class="flex flex-col md:flex-row items-center gap-16">
        <div class="md:w-1/2">
          <img src="/cactus-about.jpg" alt="Vivero" class="rounded-2xl shadow-2xl rotate-2 hover:rotate-0 transition duration-500" />
        </div>
        <div class="md:w-1/2 space-y-6">
          <h2 class="text-4xl font-bold">Más que una tienda, <br/>una pasión.</h2>
          <p class="text-emerald-100 text-lg leading-relaxed">
            En CactusWorld, nos apasiona traer la belleza del desierto a tu espacio. No solo vendemos plantas; creamos compañeros verdes que purifican tu aire y elevan tu estado de ánimo.
          </p>
          <ul class="space-y-3">
            <li class="flex items-center gap-3">
              <span class="text-emerald-400">✓</span> Asesoría personalizada
            </li>
            <li class="flex items-center gap-3">
              <span class="text-emerald-400">✓</span> Garantía de 30 días
            </li>
            <li class="flex items-center gap-3">
              <span class="text-emerald-400">✓</span> Macetas artesanales incluidas
            </li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <section id="contact" class="py-24 bg-stone-50">
    <div class="container mx-auto px-6 max-w-4xl">
      <div class="bg-white p-10 md:p-14 rounded-3xl shadow-lg border border-stone-100">
        <div class="text-center mb-10">
          <h2 class="text-3xl font-bold text-stone-900 mb-2">¿Tienes dudas?</h2>
          <p class="text-stone-500">Estamos aquí para ayudarte a elegir tu próximo cactus.</p>
        </div>
        
        <form class="space-y-6">
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div>
              <label class="block text-sm font-medium text-stone-700 mb-2">Nombre</label>
              <input type="text" class="w-full p-4 bg-stone-50 border border-stone-200 rounded-xl focus:ring-2 focus:ring-emerald-500 focus:outline-none transition" placeholder="Tu nombre" />
            </div>
            <div>
              <label class="block text-sm font-medium text-stone-700 mb-2">Email</label>
              <input type="email" class="w-full p-4 bg-stone-50 border border-stone-200 rounded-xl focus:ring-2 focus:ring-emerald-500 focus:outline-none transition" placeholder="tu@email.com" />
            </div>
          </div>
          <div>
            <label class="block text-sm font-medium text-stone-700 mb-2">Mensaje</label>
            <textarea class="w-full p-4 bg-stone-50 border border-stone-200 rounded-xl focus:ring-2 focus:ring-emerald-500 focus:outline-none h-40 transition" placeholder="¿En qué podemos ayudarte?"></textarea>
          </div>
          <button type="submit" class="w-full bg-stone-900 text-white py-4 rounded-xl font-bold hover:bg-emerald-600 transition duration-300 transform hover:-translate-y-1">
            Enviar Mensaje
          </button>
        </form>
      </div>
    </div>
  </section>
</Layout>

